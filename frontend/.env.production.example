# Esta variable se inyecta en BUILD TIME (Vite la inlinea en el bundle).
# Configurar el valor real en el dashboard de Railway > Variables del servicio "frontend".
VITE_API_BASE_URL=https://your-backend.up.railway.app/api/v1
